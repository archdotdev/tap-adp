# This file can be used to customize tox tests as well as other test frameworks like flake8 and mypy

[tox]
envlist = py3{10-14}
requires =
    tox>=4.32

[testenv]
deps =
    pytest
commands =
    pytest {posargs}
